<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Suscripciones</title>
    <!-- 
    <link rel="stylesheet" href="/css/styles.css"> 
    -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <header class="bg-primary text-white text-center py-3">
        <h1>Suscripciones</h1>
    </header>

    <div class="container my-4">
        <div class="row">
            <% if (suscripciones.length > 0) { %>
                <% suscripciones.forEach(function(suscripcion) { %>
                    <div class="col-md-4 my-2">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="card-title"><%= suscripcion.tipo %></h5>
                                <p class="card-text">
                                    <strong>ID:</strong> <%= suscripcion.id %><br>
                                    <strong>Duración:</strong> <%= suscripcion.duracion_meses %> meses<br>
                                    <strong>Porcentaje Plataforma:</strong> <%= suscripcion.porcentaje_plataforma %>%
                                </p>
                            </div>
                            <div class="card-footer text-center">
                                <a href="/suscripciones/editar/<%= suscripcion.id %>" class="btn btn-warning mx-1">Modificar</a>
                                <a href="/suscripciones/eliminar/<%= suscripcion.id %>" class="btn btn-danger mx-1">Eliminar</a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p class="text-center">No hay suscripciones disponibles.</p>
            <% } %>
        </div>
    </div>

    <footer class="bg-light text-center py-4">
        <p>&copy; 2024 - AudioLibre</p>
    </footer>

    <!-- Incluir Bootstrap JS y dependencias -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>