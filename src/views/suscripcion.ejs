<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Suscripción</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <header class="bg-primary text-white text-center py-3">
        <div class="d-flex justify-content-between align-items-center">
            <a href="/api/suscripciones" class="btn btn-light">
                <i class="bi bi-arrow-left"></i> Volver
            </a>
            <h1>Editar Suscripción</h1>
            <span></span> <!-- Espacio reservado si se necesita algo a la derecha -->
        </div>
    </header>

    <div class="container my-4">
        <h5 class="text-center">ID: <%= suscripcion.id %> | <%= suscripcion.tipo %> </h5>

        <form action="/api/suscripciones/modificar/<%= suscripcion.id %>?_method=PUT" method="POST">
            <div class="mb-3">
                <label for="tipo" class="form-label">Tipo</label>
                <input type="text" class="form-control" id="tipo" name="tipo" value="<%= suscripcion.tipo %>" required>
            </div>
            <div class="mb-3">
                <label for="duracion_meses" class="form-label">Duración (meses)</label>
                <input type="number" class="form-control" id="duracion_meses" name="duracion_meses" value="<%= suscripcion.duracion_meses %>" required>
            </div>
            <div class="mb-3">
                <label for="porcentaje_plataforma" class="form-label">Porcentaje Plataforma</label>
                <input type="number" class="form-control" id="porcentaje_plataforma" name="porcentaje_plataforma" value="<%= suscripcion.porcentaje_plataforma %>" min="0" max="100" required>
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-warning mx-1">Modificar</button>
            </div>
        </form>
        
        <div class="text-center mt-3">
            <!-- Formulario para eliminar la suscripción -->
            <form action="/api/suscripciones/eliminar/<%= suscripcion.id %>?_method=DELETE" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger mx-1">Eliminar</button>
            </form>
        </div>
    </div>

    <footer class="bg-light text-center py-4">
        <p>&copy; 2024 - AudioLibre</p>
    </footer>

    <!-- Incluir Bootstrap JS y dependencias -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css"> <!-- Para íconos de Bootstrap -->
</body>
</html>
